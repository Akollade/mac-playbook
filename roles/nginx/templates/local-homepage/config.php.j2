<?php

// {{ ansible_managed }}

$sites = [
{% for site in nginx_sites %}
    ['name' => '{{ site.name }}', 'url' => 'http://{{ site.server_name }}:{{ site.http_port|default(nginx_http_port) }}', 'path' => '{{ site.root }}'],
{% endfor %}
];

$webServerLogPath = '{{ nginx_log_path }}';
